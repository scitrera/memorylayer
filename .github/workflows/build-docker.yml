# Generated by scitrera-repo-tools `generate-ci-gha`. Do not edit by hand;
# re-run the generator after editing versions.yaml. Run
# `generate-ci-gha` (no flags) to check for drift, `generate-ci-gha --force` to apply.

name: Build & push Docker images

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Override version (e.g. 0.1.58 or v0.1.58). Empty produces sha-only tags.'
        required: false
        type: string

concurrency:
  group: docker-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  test-memorylayer-core-python:
    name: Test memorylayer-core-python (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Install
        run: pip install -e ".[dev,context,observability]"
        working-directory: memorylayer-core-python

      - name: Run tests
        run: pytest tests/ -m "not slow and not integration and not llm and not llm_quality" -x
        working-directory: memorylayer-core-python

      - name: Retrieval eval gate
        run: |
          memorylayer-eval gate
        working-directory: memorylayer-core-python

  test-memorylayer-sdk-langchain-python:
    name: Test memorylayer-sdk-langchain-python (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Install in-repo dependencies (memorylayer-sdk-python)
        run: |
          pip install -e memorylayer-sdk-python

      - name: Install
        run: pip install -e ".[dev]"
        working-directory: memorylayer-sdk-langchain-python

      - name: Run tests
        run: pytest tests/ -x
        working-directory: memorylayer-sdk-langchain-python

  test-memorylayer-sdk-llamaindex-python:
    name: Test memorylayer-sdk-llamaindex-python (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Install in-repo dependencies (memorylayer-sdk-python)
        run: |
          pip install -e memorylayer-sdk-python

      - name: Install
        run: pip install -e ".[dev]"
        working-directory: memorylayer-sdk-llamaindex-python

      - name: Run tests
        run: pytest tests/ -x
        working-directory: memorylayer-sdk-llamaindex-python

  test-memorylayer-sdk-python:
    name: Test memorylayer-sdk-python (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Install
        run: pip install -e ".[dev]"
        working-directory: memorylayer-sdk-python

      - name: Run tests
        run: pytest tests/ -x
        working-directory: memorylayer-sdk-python

  test-memorylayer-server-rpg-python:
    name: Test memorylayer-server-rpg-python (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1

      - name: Install in-repo dependencies (memorylayer-core-python)
        run: |
          pip install -e memorylayer-core-python

      - name: Install
        run: pip install -e ".[dev]"
        working-directory: memorylayer-server-rpg-python

      - name: Run tests
        run: pytest tests/ -x
        working-directory: memorylayer-server-rpg-python
  build-memorylayer-embed-server-linux-amd64:
    name: Build memorylayer-embed-server (linux/amd64)
    runs-on: ubuntu-latest
    needs: [ test-memorylayer-core-python, test-memorylayer-sdk-langchain-python, test-memorylayer-sdk-llamaindex-python, test-memorylayer-sdk-python, test-memorylayer-server-rpg-python ]
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-embed-server
            scitrera/memorylayer-embed-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}
      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: memorylayer-embed-server/Dockerfile.cpu
          platforms: linux/amd64
          labels: ${{ steps.meta.outputs.labels }}
          outputs: type=image,name=ghcr.io/scitrera/memorylayer-embed-server,push-by-digest=true,name-canonical=true,push=true
          build-args: |
            AETHER_CLIENT_VERSION=0.2.2
            AETHER_VERSION=0.2.2
          cache-from: type=gha,scope=memorylayer-embed-server-linux-amd64
          cache-to: type=gha,mode=max,scope=memorylayer-embed-server-linux-amd64
  build-memorylayer-embed-server-linux-arm64:
    name: Build memorylayer-embed-server (linux/arm64)
    runs-on: ubuntu-24.04-arm
    needs: [ test-memorylayer-core-python, test-memorylayer-sdk-langchain-python, test-memorylayer-sdk-llamaindex-python, test-memorylayer-sdk-python, test-memorylayer-server-rpg-python ]
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-embed-server
            scitrera/memorylayer-embed-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}
      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: memorylayer-embed-server/Dockerfile.cpu
          platforms: linux/arm64
          labels: ${{ steps.meta.outputs.labels }}
          outputs: type=image,name=ghcr.io/scitrera/memorylayer-embed-server,push-by-digest=true,name-canonical=true,push=true
          build-args: |
            AETHER_CLIENT_VERSION=0.2.2
            AETHER_VERSION=0.2.2
          cache-from: type=gha,scope=memorylayer-embed-server-linux-arm64
          cache-to: type=gha,mode=max,scope=memorylayer-embed-server-linux-arm64
  merge-memorylayer-embed-server:
    name: Merge memorylayer-embed-server multi-arch manifest
    runs-on: ubuntu-latest
    needs: [ build-memorylayer-embed-server-linux-amd64, build-memorylayer-embed-server-linux-arm64 ]
    permissions:
      contents: read
      packages: write
    outputs:
      base-tag: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-embed-server
            scitrera/memorylayer-embed-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}

      - name: Create multi-arch manifest
        env:
          DOCKER_METADATA_OUTPUT_JSON: ${{ steps.meta.outputs.json }}
        run: |
          TAGS=$(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON")
          eval "docker buildx imagetools create $TAGS \
            ghcr.io/scitrera/memorylayer-embed-server@${{ needs.build-memorylayer-embed-server-linux-amd64.outputs.digest }} \
            ghcr.io/scitrera/memorylayer-embed-server@${{ needs.build-memorylayer-embed-server-linux-arm64.outputs.digest }}"
  build-memorylayer-embed-server-cuda13-linux-amd64:
    name: Build memorylayer-embed-server-cuda13 (linux/amd64)
    runs-on: ubuntu-latest
    needs: [ test-memorylayer-core-python, test-memorylayer-sdk-langchain-python, test-memorylayer-sdk-llamaindex-python, test-memorylayer-sdk-python, test-memorylayer-server-rpg-python ]
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-embed-server
            scitrera/memorylayer-embed-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}
          flavor: |
            suffix=-cuda13,onlatest=true
      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: memorylayer-embed-server/Dockerfile
          platforms: linux/amd64
          labels: ${{ steps.meta.outputs.labels }}
          outputs: type=image,name=ghcr.io/scitrera/memorylayer-embed-server,push-by-digest=true,name-canonical=true,push=true
          build-args: |
            AETHER_CLIENT_VERSION=0.2.2
            AETHER_VERSION=0.2.2
          cache-from: type=gha,scope=memorylayer-embed-server-cuda13-linux-amd64
          cache-to: type=gha,mode=max,scope=memorylayer-embed-server-cuda13-linux-amd64
  merge-memorylayer-embed-server-cuda13:
    name: Merge memorylayer-embed-server-cuda13 multi-arch manifest
    runs-on: ubuntu-latest
    needs: [ build-memorylayer-embed-server-cuda13-linux-amd64 ]
    permissions:
      contents: read
      packages: write
    outputs:
      base-tag: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-embed-server
            scitrera/memorylayer-embed-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}
          flavor: |
            suffix=-cuda13,onlatest=true

      - name: Create multi-arch manifest
        env:
          DOCKER_METADATA_OUTPUT_JSON: ${{ steps.meta.outputs.json }}
        run: |
          TAGS=$(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON")
          eval "docker buildx imagetools create $TAGS \
            ghcr.io/scitrera/memorylayer-embed-server@${{ needs.build-memorylayer-embed-server-cuda13-linux-amd64.outputs.digest }}"
  build-memorylayer-server-linux-amd64:
    name: Build memorylayer-server (linux/amd64)
    runs-on: ubuntu-latest
    needs: [ test-memorylayer-core-python, test-memorylayer-sdk-langchain-python, test-memorylayer-sdk-llamaindex-python, test-memorylayer-sdk-python, test-memorylayer-server-rpg-python ]
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-server
            scitrera/memorylayer-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}
      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          labels: ${{ steps.meta.outputs.labels }}
          outputs: type=image,name=ghcr.io/scitrera/memorylayer-server,push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=memorylayer-server-linux-amd64
          cache-to: type=gha,mode=max,scope=memorylayer-server-linux-amd64
  build-memorylayer-server-linux-arm64:
    name: Build memorylayer-server (linux/arm64)
    runs-on: ubuntu-24.04-arm
    needs: [ test-memorylayer-core-python, test-memorylayer-sdk-langchain-python, test-memorylayer-sdk-llamaindex-python, test-memorylayer-sdk-python, test-memorylayer-server-rpg-python ]
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-server
            scitrera/memorylayer-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}
      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/arm64
          labels: ${{ steps.meta.outputs.labels }}
          outputs: type=image,name=ghcr.io/scitrera/memorylayer-server,push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=memorylayer-server-linux-arm64
          cache-to: type=gha,mode=max,scope=memorylayer-server-linux-arm64
  merge-memorylayer-server:
    name: Merge memorylayer-server multi-arch manifest
    runs-on: ubuntu-latest
    needs: [ build-memorylayer-server-linux-amd64, build-memorylayer-server-linux-arm64 ]
    permissions:
      contents: read
      packages: write
    outputs:
      base-tag: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Compute workflow_dispatch version tags
        id: ver
        if: github.event_name == 'workflow_dispatch' && inputs.version != ''
        shell: bash
        run: |
          VER="${{ inputs.version }}"
          VER="${VER#v}"
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "major_minor=$(echo $VER | cut -d. -f1-2)" >> "$GITHUB_OUTPUT"

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/scitrera/memorylayer-server
            scitrera/memorylayer-server
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=${{ steps.ver.outputs.version }},enable=${{ steps.ver.outputs.version != '' }}
            type=raw,value=${{ steps.ver.outputs.major_minor }},enable=${{ steps.ver.outputs.major_minor != '' }}
            type=raw,value=latest,enable=${{ steps.ver.outputs.version != '' }}

      - name: Create multi-arch manifest
        env:
          DOCKER_METADATA_OUTPUT_JSON: ${{ steps.meta.outputs.json }}
        run: |
          TAGS=$(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON")
          eval "docker buildx imagetools create $TAGS \
            ghcr.io/scitrera/memorylayer-server@${{ needs.build-memorylayer-server-linux-amd64.outputs.digest }} \
            ghcr.io/scitrera/memorylayer-server@${{ needs.build-memorylayer-server-linux-arm64.outputs.digest }}"
