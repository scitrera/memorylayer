[project]
name = "memorylayer-embed-server"
version = "0.2.0"
description = "MemoryLayer.ai Embed Server - Stateless transcription and embedding GPU server"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache 2.0" }
authors = [
    { name = "scitrera.ai", email = "open-source-team@scitrera.com" }
]
keywords = ["ai", "memory", "embedding", "transcription", "gpu", "glm-ocr", "vllm", "colpali"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "memorylayer-server==0.2.0",
    "fastapi==0.136.1",
    "uvicorn[standard]==0.46.0",
    "pydantic==2.13.4",
    "click==8.3.3",
    "numpy>2.0.0",
    "Pillow>=10.0.0",
]

[project.optional-dependencies]
# Local/CPU single-vector embeddings (sentence-transformers, all-MiniLM-L6-v2).
# This backs the DEFAULT single-vector provider, so it is what a local install
# needs: pip install "memorylayer-embed-server[local]". Kept an extra rather than
# a base dependency because it pulls torch, and deployments that only use the
# openai/google passthrough should not pay for that.
local = ["sentence-transformers>=3.0.0", "torch>=2.11.0,<2.12.0"]
ocr = ["transformers>=5.5.0", "torch>=2.11.0,<2.12.0", "accelerate>=0.26.0", "addict", 'matplotlib']
vllm = ["vllm>=0.19.0", "instanttensor"]
# Launch the vLLM lanes through sparkrun (recipes + local executor) instead of
# our own subprocess runner. GPU-path only — deliberately NOT in `local`, so the
# CPU image does not carry it.
sparkrun = ["sparkrun>=0.3.2"]
colpali = ["colpali-engine>=0.3.0", "torch>=2.11.0,<2.12.0"]
google = ["google-genai>=2.3.0"]
# Observability: Prometheus /metrics endpoint + OpenTelemetry SDK / OTLP
# exporter. The Prometheus exposition is the simplest OTel-compatible
# integration point — an OTel collector can scrape /metrics and forward
# anywhere; auto-instrumentation for FastAPI / httpx provides traces.
observability = [
    "prometheus-client>=0.20.0",
    "opentelemetry-sdk>=1.27.0",
    "opentelemetry-exporter-otlp-proto-grpc>=1.27.0",
    "opentelemetry-instrumentation-fastapi>=0.48b0",
    "opentelemetry-instrumentation-httpx>=0.48b0",
]
gpu = ["memorylayer-embed-server[ocr,vllm,colpali]"]
all = ["memorylayer-embed-server[local,gpu,google,observability]"]
# Visual-tokenizer (Qwen3.5) lives in the proprietary
# memorylayer-embed-server-enterprise package; install that separately.
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=7.0.0",
    "ruff>=0.15.0",
]

[project.scripts]
memorylayer-embed = "memorylayer_embed_server.cli:cli"

[project.urls]
Homepage = "https://memorylayer.ai"
Documentation = "https://docs.memorylayer.ai"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
# Package builders must exclude local state even when building from a dirty checkout.
exclude = [
    "**/.omc/**", "**/.claude/**", "**/.codex/**", "**/.agents/**", "**/.slop/**",
    "**/.venv/**", "**/__pycache__/**", "**/node_modules/**",
    "**/.env", "**/.env.*", "**/.mcp.json",
    "**/*.db", "**/*.db-shm", "**/*.db-wal", "**/*.sqlite", "**/*.sqlite3",
    "**/*.pem", "**/*.key", "**/*.pyc",
]

[tool.hatch.build.targets.wheel]
packages = ["src/memorylayer_embed_server"]

[tool.hatch.build.targets.sdist]
include = ["src/memorylayer_embed_server"]

[tool.ruff]
line-length = 140
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
# Default selection excludes slow + integration; opt-in with `-m slow` or
# `-m integration` to run real-model and docker chain tests.
addopts = "-m 'not slow and not integration'"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
]

[tool.mypy]
python_version = "3.12"
strict = false
warn_return_any = true
warn_unused_configs = true
